
.wrapper_with_padding
  #doc_show
    %h1= @doc.title
    %p= simple_format(@doc.content)

    .buttons

      =link_to "Fix", edit_doc_path(@doc), class: "button"
      =link_to "Trash", doc_path(@doc), method: :delete, data: {confirm: "are you sure?"} , class: "button"
